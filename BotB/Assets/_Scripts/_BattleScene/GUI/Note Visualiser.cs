using UnityEngine;
using System.Collections;

public class NoteVisualiser : MonoBehaviour 
{
    bool m_putDown = false;
    //Note m_noteType;
	// Use this for initialization
	void Start()
    {

	}
	
	// Update is called once per frame
	void Update()
    {
        if(m_putDown)
        {

        }
	}/*

    void ReceiveNote(TimedNote a_note)
    {
        m_putDown = true;
        m_noteType = a_note.note;
    } */
}
